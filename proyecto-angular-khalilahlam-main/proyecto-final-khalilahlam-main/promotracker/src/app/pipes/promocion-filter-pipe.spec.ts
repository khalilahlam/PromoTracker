import { PromocionFilterPipe } from './promocion-filter-pipe';

describe('PromocionFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PromocionFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
